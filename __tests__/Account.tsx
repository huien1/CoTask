import Account from '../pages/Account';
import { render, screen, fireEvent } from '@testing-library/react-native';

// jest.useFakeTimers();

test('account page renders correctly', () => {
    render(<Account />);
});





